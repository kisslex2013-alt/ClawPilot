# Dashboard App

The dashboard app is not implemented yet.

## v1 data it should show

- task state
- current phase
- blocker / approval state
- run artifacts
- links to ClawLoop report surfaces

## Note

The existing ClawLoop markdown dashboard is a source input, not the final UI.
